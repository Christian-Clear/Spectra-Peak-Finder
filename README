This is a python script which finds the wavenumber and max. SNR of peaks in an FTS spectrum. 

To make the file executable from anywhere on the system:

1. cp peakfind.py /usr/local/bin

2. mv peakfind.py ftspeakfind

3. chmod 777 ftspeakfind

*Note: will need to sudo for most of these*
